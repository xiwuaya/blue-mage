# FFXIV 地图集成开发文档

> **状态：已实现并验证完成。**
> 本文档已从"规划稿"更新为"实现后的最新状态"。
> 凡是标注 **【已验证】** 的结论，都来自实测（读 `dist/map.js` 源码、真实网络请求、或浏览器渲染验证），
> 不是推断。凡是标注 **【纠正】** 的，是初版文档写错、现已改正的地方。

---

# 1. 项目目标

蓝魔法技能查询页面中，部分技能的获取途径带地图坐标，例如：

```json
{
  "no": "8",
  "action": 11407,
  "patch": "4.5",
  "spell": "终极针",
  "level": 13,
  "method": [
    {
      "type": "map",
      "map": "中拉诺西亚",
      "rank": null,
      "position": [15, 15, 0],
      "mob": "杀手胡蜂",
      "level": 13,
      "color": "yellow"
    }
  ]
}
```

目标：**点击途径后面的地图图标 → 弹出交互式地图（Waking Sands / 神典石）→ 定位到对应地图 → 在坐标处放置标记。**

**该目标已实现。** 见第 8 节。

---

# 2. 最终数据链路（已实现）

```text
spells.json
    │
    ▼
method.type === "map"
    │
    ├── method.map ──────┐
    │                    │
    │                    ▼
    │         tools/map-keys.json（构建时生成，静态表）
    │                    │  未命中
    │                    ▼
    │         getRegion()（地图库自带索引，零额外请求）
    │                    │
    │                    ▼
    │              map key（数字）
    │                    │
    │                    ▼
    │            map.loadMapKey(key)
    │
    └── method.position
            │
            │  已经是游戏内显示坐标，直接使用
            ▼
        simpleMarker(position[0], position[1], iconUrl, map.mapInfo)
            │
            ▼
        map.addMarker(marker)
            │
            ▼
        map.setView(map.mapToLatLng2D(x, y), 0)
```

**核心规则：`method.position` 直接作为坐标使用，不做任何换算。**
这条规则经源码验证是正确的，详见第 3 节。

---

# 3. 坐标格式 **【已验证】**

```json
"position": [15, 15, 0]
```

语义：

```text
position[0] = 游戏内显示坐标 X
position[1] = 游戏内显示坐标 Y
position[2] = 保留字段，地图 marker 定位不使用
```

## 3.1 为什么不需要坐标转换

`simpleMarker` 的内部实现是：

```js
function simpleMarker(x, y, iconUrl, mapInfo) {
  icon = new Icon({ iconSize: new Point(32, 32), iconUrl });
  return new Marker(xy(fromMapXY2D(mapInfo, x, y)), { icon, zIndexOffset: 1000, pane: "popupPane" });
}
```

其中 `fromMapXY2D` 就是**把游戏内显示坐标换算成 Leaflet 内部坐标**：

```js
// an(t, sizeFactor) = (t - 1) / (4100 / sizeFactor) * 2048
// un(mapInfo, x, y) = [an(x, sizeFactor), an(y, sizeFactor)]
```

从 `4100 / sizeFactor` 这个除数可以反推出：**显示坐标的量级是 1–42**。
而 `spells.json` 中全部 81 个 map 途径的 `position` 实测落在 7–37 之间（含 `[26.7, 20.9, 0]` 这类小数），完全吻合。

所以：

```text
position[0] / position[1]
    ↓
直接传给 simpleMarker
```

**不要调用 `toMapXY3D`。** 那是给世界坐标用的，会对已经转换过的坐标做第二次转换。
`toMapXY3D` 只使用 `sizeFactor` / `offset{X}` / `offset{Y}`，与显示坐标不是同一个坐标系。

## 3.2 数据完整性 **【已验证】**

实测统计：81 个 `type === "map"` 的途径，**全部**有 `map` 字段，且 `position` 都是长度为 3 的纯数字数组。
不需要空值兜底，但代码里仍建议保留判断以防后续数据变化。

---

# 4. 地图名 → map key 的解析

## 4.1 【纠正】初版方案是运行时调 XIVAPI，现已改为静态表 + 本地兜底

初版文档（本文件的历史版本）设计的是：点击图标时 fetch XIVAPI 中文镜像，用返回的 `Map.row_id` 调 `loadMapKey`。

**这个方案能用，但不是最优的**，原因见 4.2。现在的实现是：

1. **构建时**用 XIVAPI 生成静态表 `tools/map-keys.json`（31 条），打进包里；
2. **运行时**优先查静态表，查不到才用地图库自带的 `getRegion()` 索引兜底；
3. **运行时完全不调用 XIVAPI。**

好处：零运行时网络依赖、零延迟、不受第三方接口可用性影响，且静态表可在构建时人工核对。

## 4.2 三个数据源的关系 **【已验证】**

用两个**互相独立**的数据源交叉验证了全部 31 个唯一地图名：

| 数据源 | 结果 |
| --- | --- |
| XIVAPI `Map.row_id`（`PlaceNameSub.row_id === 0` 规则） | 30/31 唯一解析 |
| 地图库自带 `getRegion()` 返回的 `key` | 30/31 唯一解析 |
| **两者交叉对比** | **30 个全部完全一致，0 处分歧** |

两个独立来源 100% 吻合，说明 map key 空间是可靠的。

> 补充：`create()` 内部本身就会 `await getRegion()` 去拉 `region.json`，
> 拿不到这份数据地图根本建不起来。所以用 `getRegion()` 兜底**不产生任何额外网络请求**。

## 4.3 XIVAPI 的 CORS **【已验证】**

初版文档只用 curl 验证过 XIVAPI，没验证浏览器跨域。实测：

```text
请求：https://xivapi-v2.xivcdn.com/api/search?...&language=chs
带 Origin 头
响应头：Access-Control-Allow-Origin: *
```

浏览器 `fetch` 不会被 CORS 拦截。

## 4.4 `region.json` 的重名问题 **【已验证】**

`getRegion()` 返回 1073 条记录，其中 **534 条重名**。直接按名字建索引会命中错误条目。

必须过滤：

```ts
// 只保留"主地图"
const isMainMap = (m) =>
  !m.subName &&                       // subName 非空的是子区域，如「黑衣森林南部林区 - 兀尔德泉」
  !m.id.startsWith("default") &&      // id 以 default/ 开头的是占位条目（如 key 187）
  !m.id.startsWith("region");         // id 以 region/ 开头的是区域级条目（如 key 91）
```

即便过滤后仍有歧义 —— `摩杜纳` 有 3 个候选（key 25 / 91 / 105）。
实现中取**最小 key**，实测与 XIVAPI 的判定结果一致（→ 25，即 `l1f1/01`）。

这也是保留静态表的理由之一：构建时可以人工核对，不必依赖运行时的启发式规则。

## 4.5 【已验证】数据错字

`spells.json` 里原本写的是 `库尔扎斯西部高地`，而游戏官方地名是 **`库尔札斯西部高地`**（`札` 而非 `扎`）。

差这一个字，导致 XIVAPI 和 `region.json` **两个源都查不到**。已修正 `tools/spells.json`。

该错字在仓库中只出现过 1 处，已修复。修正后正确解析为 `key = 211`。

`tools/update-map-keys.js` 会在构建时对解析失败的地名 `console.warn` 并汇总，
以便**下一个错字在构建时就暴露**，而不是运行时静默失败。

---

# 5. Waking Sands npm 包：实际 API

以下全部来自对 `node_modules/@thewakingsands/eorzea-interactive-map/dist/map.js`（170KB 压缩 UMD）的**实际阅读**。

> 注：`package.json` 里写 `version: 1.1.3`，但产物内部导出的 `version` 是 `"1.1.2"`，两者不一致。

## 5.1 【纠正】导入方式：不要用 `window.YZWF.eorzeaMap`

初版文档建议用 `window.YZWF.eorzeaMap.create(...)`。**这在 Vite 下不成立。**

UMD 包装头：

```js
((t, e) => {
  "object" == typeof exports && "undefined" != typeof module ? e(exports)   // ← CJS 分支
  : "function" == typeof define && define.amd ? define(["exports"], e)
  : e((t.YZWF = t.YZWF || {}, t.YZWF.eorzeaMap = {}));                      // ← 浏览器 <script> 分支
})(this, function (t) { ... });
```

`window.YZWF.eorzeaMap = {}` **只在浏览器 `<script>` 全局分支执行**。
Vite 走 CJS 互操作分支，只会执行包内那句无条件的 `window.YZWF = window.YZWF || {}` ——
**`window.YZWF` 存在，但 `.eorzeaMap` 是 `undefined`。**

正确写法：

```ts
import * as eorzeaMap from "@thewakingsands/eorzea-interactive-map";
// 或者具名导入
import { create, simpleMarker, loader, getRegion } from "@thewakingsands/eorzea-interactive-map";
```

注意：

- 具名导入**可用**（esbuild 的 `__toESM` 在运行时从 exports 对象拷贝属性）；
- **不要用默认导入** —— UMD 设了 `__esModule: true`，`default` 是 `undefined`；
- 包没有 `types` 字段，需要自备 `.d.ts`（本项目放在 `src/types/eorzea-map.d.ts`）。

## 5.2 【已验证】`addMarker` 才是正确方法名

初版文档把它列为"未解决问题"。源码已确认：

```js
B.prototype.addMaker = function (t) {
  console.warn("[Deprecated] map.addMaker is a misspell, you should use map.addMarker instead.");
  return this.addMarker(t);
};
B.prototype.addMarker = function (t) {
  return t.addTo(this), this.markers.push(t), t;
};
```

**用 `map.addMarker(marker)`。** `addMaker` 是已废弃的拼写错误。

## 5.3 `create(el)`

```js
async function create(el) {
  el.innerHTML = "";                                     // 会清空容器
  const map = new L.Map(el, {
    crs: L.CRS.Simple,
    minZoom: -3, maxZoom: 4,
    attributionControl: false, zoomControl: false, inertiaMaxSpeed: 5000,
  });
  Object.setPrototypeOf(map, EorzeaMap.prototype);
  const regions = await getRegion();                      // ← 网络请求，失败即 reject
  map.init(regions, el);
  attachClickHandler(el, map);                            // 在容器上挂 click 监听（永不解除）
  return map;
}
```

要点：

- **返回 Promise**；
- **会清空容器 `innerHTML`**；
- **不提供任何宽高样式** —— 容器必须自己给尺寸；
- **内部 `await getRegion()` 拉 `region.json`，拿不到就 reject**，没有内置兜底 → 必须处理失败态；
- 容器尺寸为 0×0 时，`fitBounds` → `getSize()` 会退化成错误视图。

## 5.4 `loadMapKey(key)`

```js
async loadMapKey(key) {
  const info = (await getMapJson())[key];   // map.json 被 memoize，整个页面只拉一次
  this.fire("loadMapKey", { mapKey: key });
  return this.loadMapInfo(info);
}
```

`loadMapInfo` 的关键行为：

```js
this.mapInfo = l;                          // ← 同步赋值
if (this.markers.length) { this.markers.forEach(m => m.remove()); this.markers = []; }  // ← 清空已有标记！
if (this.overlays.length) { ... this.overlays = []; }
this.tileLayer && (this.tileLayer.remove(), this.tileLayer = null);
...
this.panTo(...);                           // ← 重新居中到地图中心
this.fire("updateInfo", { mapInfo: l });
```

**最重要的结论：`loadMapKey` 会清空 `markers`，所以标记必须在这之后再加。**

替换地图（用不同 key 再调一次）是安全的，它会自行清理上一次的 markers / overlays / tileLayer。

## 5.5 `simpleMarker` 与居中

```js
const iconUrl = eorzeaMap.loader.getIconUrl("ui/icon/060000/060561.tex");
map.addMarker(eorzeaMap.simpleMarker(x, y, iconUrl, map.mapInfo));
map.setView(map.mapToLatLng2D(x, y), 0);
```

`mapToLatLng2D(x, y)` = `xy(fromMapXY2D(x, y))` —— 与 `simpleMarker` 内部**完全相同的换算**，
所以两者天然一致，用于把标记居中。

## 5.6 【已验证】`setView` 是异步生效的

实测：调用 `setView(latlng, 0)` 后**立刻**读 `map.getCenter()`，拿到的仍是旧值；约 800ms 后才是目标值。

原因是库自身的 `panTo`（把视图重置到地图中心）还在进行中，`setView` 会排在它之后生效。

**结论：最终状态是正确的，不需要额外处理。** 但如果要在页面上立刻读取或二次操作视图，必须考虑这个延迟。
库自己的深链代码用的是 `setTimeout(() => map.setView(...), 100)`，也是同一个原因。

## 5.7 缩放范围 **【已验证】**

```text
合法范围：-3 .. 4
-1 = 整图概览（create 后的默认值）
 0 = 原生图块分辨率，库内深链定位用的值
```

本项目用 `0`。

## 5.8 清理 **【已验证】**

- 地图类**没有**自己的 `destroy` / `unload` / `dispose`；
- 但继承了 Leaflet 的 **`map.remove()`**，会注销 `window` resize 监听、移除 pane/control，
  并清除容器的 `_leaflet_id`（不清除它，下次在同一元素上 `create()` 会抛
  `Map container is already initialized.`）；
- **`map.remove()` 不会清空 innerHTML**，但 `create()` 会。

⚠️ **`create()` 内部 `attachClickHandler(el, map)` 挂在容器上的 click 监听器永不解除。**
所以不要反复在**同一个 DOM 元素**上 create/remove —— 旧闭包会一直持有旧地图。
本项目的做法是靠 `v-if` 让容器在关闭时被销毁、下次打开时重建，从而拿到全新的元素。

## 5.9 `loader`

```js
loader = {
  getMapUrl, getBgUrl, getTileUrl, getIconUrl, parseIcon, setBaseUrl, setUrlFunction,
  CDN_SERVER, NULL_ICON_GROUP,
}
```

- `getIconUrl("ui/icon/060000/060561.tex")`
  → `https://xivapi-v2.xivcdn.com/api/asset?path=ui/icon/060000/060561.tex`（实测 200）；
- `parseIcon` 的正则是 `/^ui\/icon\/(\d{6})\/(\d{6})\.tex/`；
- **没有任何 map 名字相关的查询接口**（没有 `getMapList`、没有 name 访问器）。

默认 CDN：`https://map.ffcafe.cn/assets`

## 5.10 `getRegion()` —— 官方文档没写的"地图名字典" **【已验证】**

这是本次开发最有价值的发现：

```js
getRegion() → [
  {
    regionName: "艾欧泽亚",
    maps: [
      { id: "world/00", key: 92, hierarchy: 2, name: "艾欧泽亚", subName: "", regionName: "艾欧泽亚" },
      { id: "s1f1/00",  key: 15, hierarchy: …, name: "中拉诺西亚", subName: "", regionName: "拉诺西亚" },
      ...
    ]
  },
  ...
]
```

- 数据源：`https://map.ffcafe.cn/assets/data/region.json`（约 203KB，`Access-Control-Allow-Origin: *`）；
- **内部 memoize**，整个页面只发一次请求 —— 而且 `create()` 本来就会调它；
- 共 1073 条记录、539 个唯一名字（重名严重，见 4.4）。

**这意味着「中文地图名 → map key」根本不需要 XIVAPI**，库自己就带着这份数据。

## 5.11 `map.mapInfo` 字段

```js
{
  "#": "15",              // 数字 key，以字符串形式存放
  id: "s1f1/00",          // 地图资源 id
  sizeFactor: 100,        // 坐标换算除数
  "offset{X}", "offset{Y}",  // 仅 toMapXY3D 使用
  placeName: "中拉诺西亚",
  "placeName{Sub}": "",
  mapMarkerRange: …,      // 内置 POI 过滤用
}
```

`loadMapKey` 的 await 完成后 `mapInfo` 一定已就绪。

## 5.12 库会自动加载自己的 POI 标记

`loadMapKey` 之后，地图上会出现该地图自带的兴趣点标记（实测：中拉诺西亚 25 个、黑衣森林中央林区 45 个、
库尔札斯西部高地 38 个）。**你添加的标记是在这些之上额外叠加的**，
所以判断"自己的标记是否渲染成功"时，不能只看标记总数，要按图标 URL 区分。

## 5.13 已知 bug：不要直接调用 `xy(x, 0)`

```js
function xy(t, e) { const i = e ? t : t[0]; return [2048 - (e || t[1]), i]; }
```

`xy(x, 0)` 会因为 `0` 是 falsy 而走到错误分支，返回 `[NaN, undefined]`。
`simpleMarker` / `mapToLatLng2D` 内部都是传数组，**不受影响**；但不要自己直接调 `xy(x, y)`。

---

# 6. 样式：必须额外引入 `leaflet.css` **【文档原缺口】**

初版文档完全没提这件事，是本次发现的最大实现陷阱。

- `@thewakingsands/eorzea-interactive-map/dist/map.css` **只有 56 条规则**，
  其中与 leaflet 相关的仅 `.leaflet-container`、`.leaflet-tooltip-pane` 等 4 个类；
- Leaflet 的**核心布局类**（`.leaflet-pane` / `.leaflet-tile` / `.leaflet-map-pane` /
  `.leaflet-zoom-animated` / `.leaflet-marker-icon`）**一个都没有**；
- `map.js` **不会**在运行时注入任何样式（已确认没有 `createElement("style")` / `insertRule`）。

**只引 `map.css` 的话，地图会塌成一堆叠在一起的图块。**

正确做法（顺序要紧，leaflet 在前）：

```ts
import "leaflet/dist/leaflet.css";
import "@thewakingsands/eorzea-interactive-map/dist/map.css";
```

`leaflet` 是地图包的依赖，会被提升到 `node_modules/leaflet`，但**本项目已把它显式写进
`package.json` 的 dependencies**，避免依赖提升这个巧合。

> 补充：Leaflet 内部有 `document.querySelector('link[href$="leaflet.css"]')` 用来推断默认图标路径。
> 经 Vite 打包后该文件名会变成 `index.<hash>.css`，这个选择器匹配不上 —— 但它只影响
> `L.Icon.Default` 的默认图标，本项目所有标记都显式传 `iconUrl`，**不受影响**。

---

# 7. 实现结构

## 7.1 文件清单

| 文件 | 作用 |
| --- | --- |
| `tools/update-map-keys.js` | 构建时脚本：地名 → key，生成静态表 |
| `tools/map-keys.json` | 生成的静态表（31 条），构建产物，随仓库提交 |
| `src/lib/map.ts` | 运行时 `resolveMapKey()`：静态表优先，`getRegion()` 兜底 |
| `src/types/eorzea-map.d.ts` | 手写类型声明（包本身没有 types） |
| `src/components/MapModal.vue` | 地图弹窗组件 |
| `src/components/Method.vue` | 地图图标 + 点击抛事件 |
| `src/components/SpellItem.vue` | 转发事件（补上技能名） |
| `src/components/SpellList.vue` | 转发事件 |
| `src/App.vue` | 持有 `mapTarget`，挂载 `MapModal` |

## 7.2 弹窗生命周期

顺序很关键：

```text
watch(show, true)
  → await nextTick()            容器必须已插入且有尺寸
  → await create(el)            内部 el.innerHTML=""，并拉 region.json
  → await resolveMapKey(name)   静态表 → getRegion() 兜底
  → await loadMapKey(key)       会清空 markers，所以标记要在这之后加
  → addMarker(simpleMarker(x, y, iconUrl, map.mapInfo))
  → setView(mapToLatLng2D(x, y), 0, { animate: false })
  → 收起 loading
关闭
  → 自增 token 让飞行中的异步流程失效
  → map.remove()
```

### 为什么 `setView` 必须带 `animate: false`

库在 `loadMapKey` 内部会先 `panTo` 到地图中心，本项目的 `setView` **排在它之后才生效**。

如果保留平移动画，视图会有一段**肉眼可见的"慢慢挪过去"**的过程
（实测单实例约 800ms；两个地图同时加载时，7 秒时标记仍偏着 162px），
用户会看到标记先偏在一边、再飘到正确位置。

实现上试过"用 `moveend` + 中心点比对来决定何时收起加载遮罩"，**已否决**：
它要么在库自己那次 `panTo` 后就收起（没盖住），要么得靠超时兜底，
而超时一旦先到就又漏出飘移（实测 400px 下确实漏了）。**关掉动画是一步到位，
根本不存在那段中间状态**，比"想办法把它遮住"更简单也更可靠。

## 7.3 事件链路

沿用项目既有的 `search` 事件四层链路：

```text
Method.vue  emit("openMap", method)
    ↓
SpellItem.vue  emit("openMap", { spellName, method })   ← 技能名在 Spell 上，不在 SpellMethod 上
    ↓
SpellList.vue  emit("openMap", $event)
    ↓
App.vue  @openMap="mapTarget = $event"  →  <MapModal v-if="mapTarget" />
```

`App.vue` 用 `v-if` 而不是 `:show` 挂载，是为了确保**每次打开都是一个全新的容器 DOM 元素**（见 5.8）。

---

# 8. 踩过的坑

## 8.1 Vite 会把静态 `src` 当成模块导入

```html
<!-- ✗ 构建直接失败 -->
<img src="icons/map.svg" />
```

```text
[vite]: Rollup failed to resolve import "icons/map.svg" from "src\components\Method.vue"
```

Vite 会把**静态** `src` 属性重写成模块导入，而 `public/` 下的路径无法被这样解析。

```html
<!-- ✓ 用动态绑定绕开（项目里已有的写法也是这样） -->
<img :src="mapIcon" />
```

## 8.2 Vue 模板 ref 的类型陷阱

本项目用的是 **Vue 3.2.47 + Volar 0.31.4 + TypeScript 4.5.5**。
在这个组合下，字符串模板 ref 会让 `xxx.value` 被推断成一份结构上"像"`HTMLElement`
但并不等同的类型，传给库的 `create(el)` 会报：

```text
error TS2345: Argument of type '{ accessKey: string; ... 274 more ... }'
is not assignable to parameter of type 'HTMLElement'.
```

**改用函数 ref 可解：**

```ts
import type { ComponentPublicInstance } from "vue";

let mapEl: HTMLElement | null = null;
const setMapElement = (el: Element | ComponentPublicInstance | null) => {
  mapEl = el as HTMLElement | null;
};
```

```html
<div :ref="setMapElement" class="map-container"></div>
```

> 注意函数 ref 的签名必须包含 `ComponentPublicInstance`，否则 Volar 会报
> `Type '(el: Element | null) => void' is not assignable to type 'VNodeRef | undefined'`。

## 8.3 其他要点回顾

| 坑 | 结论 |
| --- | --- |
| 标记加早了 | `loadMapKey` 会清空 `markers`，必须在 await 之后再加 |
| 容器没尺寸 | 库不提供宽高；0×0 会让视图计算退化 |
| 容器复用 | 库在容器上挂的 click 监听永不解除 → 每次开弹窗用新元素 |
| `setView` 立刻读 | 异步生效，约 800ms 后才稳定 |
| 地名错字 | 构建时 warn，别等运行时静默失败 |
| 图标路径 | `public/` 资源在子路径部署下要用**相对路径**，绝对 `/icons/...` 会 404 |

---

# 9. 最终架构

```text
┌──────────────────────────────────────────────┐
│                 spells.json                  │
└───────────────────┬──────────────────────────┘
                    │
        method.type === "map"
                    │
     ┌──────────────┴───────────────┐
     ▼                              ▼
 method.map                   method.position
     │                              │
     ▼                              │  （已是显示坐标，不转换）
 ┌─────────────────────┐            │
 │ 构建时：XIVAPI       │            │
 │  ↓                  │            │
 │ tools/map-keys.json │            │
 └─────────┬───────────┘            │
           │ 命中                    │
           ├──────────► map key ◄────┼─── 未命中
           │                         │     │
           │                         │     ▼
           │                         │  getRegion()（已 memoize，零额外请求）
           │                         │
           ▼                         │
    map.loadMapKey(key)              │
           │                         │
           ▼                         │
       map.mapInfo ◄─────────────────┤
           │                         │
           └──────────┬──────────────┘
                      ▼
        simpleMarker(x, y, iconUrl, mapInfo)
                      │
                      ▼
              map.addMarker()
                      │
                      ▼
        map.setView(mapToLatLng2D(x, y), 0)
```

---

# 10. 已确认数据表

| 项目 | 内容 |
| --- | --- |
| npm 包 | `@thewakingsands/eorzea-interactive-map` |
| package.json 版本 / 产物实际版本 | `1.1.3` / `1.1.2`（不一致） |
| 主文件 / CSS | `dist/map.js` / `dist/map.css` |
| **必须额外引入** | **`leaflet/dist/leaflet.css`** |
| 导入方式 | `import * as eorzeaMap from "@thewakingsands/eorzea-interactive-map"` |
| ~~全局对象~~ | ~~`window.YZWF.eorzeaMap`~~ **【纠正】Vite 下不可用** |
| 创建地图 | `create(element)` → Promise |
| 加载地图 | `loadMapKey(key)` → Promise |
| 添加标记 | **`addMarker()`**（`addMaker` 是废弃拼写） |
| 坐标换算 | **不使用** `toMapXY3D` |
| `method.position` | **直接作为显示坐标使用** |
| 默认缩放 | `-1` 整图；本项目用 `0` |
| 缩放范围 | `-3 .. 4` |
| 图标 | `loader.getIconUrl("ui/icon/060000/060561.tex")` |
| 地名字典 | `getRegion()` → `region.json`（1073 条 / 539 唯一名） |
| 默认 CDN | `https://map.ffcafe.cn/assets` |
| 图标 CDN | `https://xivapi-v2.xivcdn.com/api/asset?path=ui/icon/` |
| XIVAPI 镜像 | `https://xivapi-v2.xivcdn.com`（`language=chs`，CORS `*`） |
| 中拉诺西亚 | `15` |
| 库尔札斯西部高地 | `211` |
| 摩杜纳 | `25` |
| 黑衣森林南部林区 | `6` |

---

# 11. 部署前提与既有情况

## 11.1 `base: "/"` 是对的，站点以根域为前提 **【已澄清】**

> 本节曾写作"线上站点与仓库不一致，下次部署会整站 404"，**那个结论是错的**，已更正。

`vite.config.ts` 的 `base: "/"` 是**有意为之**：项目正从子路径迁移到根域部署，
`base` 已于 commit `e73f7f8`（2026-03-27）从 `/blue-mage/` 改成 `/`。

实测线上仍是旧版本（页面标题是「青魔法师技能学习指南」，而仓库 `index.html` 已是「青魔法来源查询」），
引用的是 `/blue-mage/assets/...` —— 但这只是**线上尚未重新部署**，不是仓库配错了。
当前 `base` 无需修改，**后续改动都应以根域为前提**。详见 `docs/adr/0002-base-root-path.md`。

## 11.2 public 资源用绝对路径或相对路径都可以 **【已更正】**

曾经把 `PartyModal.vue` / `SpellList.vue` 里的 `mask: url('/icons/Visible.svg')`
判为"子路径下会 404 的既有 bug"——**在根域前提下这是错的**，它工作正常，不是问题。

（本功能里 `Method.vue` 用的是相对路径 `icons/map.svg`，两种写法在根域下都对。
但**必须用动态绑定 `:src`**，否则 Vite 会把静态 `src` 当模块导入，构建直接失败，见 8.1。）

## 11.3 `hunt` / `levequests` 目前**无法**接入地图入口（数据缺失）

不是设计选择，是数据不够 —— 实测：

| type | 途径数 | 有 map | 有可用 position |
| --- | ---: | ---: | ---: |
| `map` | 81 | 81 | **81** |
| `hunt` | 28 | 28 | **2**（另 20 条是空数组 `[]`，6 条无此字段） |
| `levequests` | 4 | 4 | **0** |

`position: []` 是**有意为之**（`spell.ts` 里 `pos && pos.length ? … : ""` 就是为它写的），
表示"坐标未知"。所以要扩展得先补数据，不是改 `Method.vue` 的 `v-if` 就行。

---

# 12. 后续可扩展方向

- **一个地图多个技能**：`loadMapKey` 会对同一 key 重复加载；若同地图标记很多，
  可以考虑合并到一次加载里再加多个 marker。
- **标记信息浮层**：文档早期设想的「技能名 / 怪物 / 等级」已实现在弹窗顶部的信息栏；
  如果要在 marker 上直接显示，可用库的 `mapMarkerRange` / tooltip 相关能力。
- **子地图**：`region.json` 里带 `subName` 的条目目前被过滤掉了（如「黑衣森林南部林区 - 兀尔德泉」）。
  如果某些数据需要定位到子区域，需要扩展 `isMainMap` 的判定。
- **静态表缓存策略**：`tools/update-map-keys.js` 已做了「名字没变就沿用旧值」的优化，
  正常构建只会在新增地名时发请求。
