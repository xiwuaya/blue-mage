# 青魔法来源查询

一个查询 FFXIV 青魔法师技能获取途径的网页。数据的核心是「技能」和它的「途径」，
而途径分成若干种，其中只有带坐标的那几种能落到地图上。

## Language

**地图途径**:
`type === "map"` 的途径——野外的具体怪物点位，带 `map` 和 `position`。
_Avoid_: 野怪途径、坐标途径

**坐标途径**:
`type` 为 `map`、`hunt`、`levequests` 之一的途径集合。它们在类型上都落到 `SpellMethodMap`，
都可能有 `map` 和 `position`，但**只有 `map` 的数据是完整的**（`hunt` 28 条里只有 2 条有可用坐标，
`levequests` 4 条一条都没有），所以目前只有「地图途径」接入了地图入口。
_Avoid_: 地图途径（两者不是一回事）
